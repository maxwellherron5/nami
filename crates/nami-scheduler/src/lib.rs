//! Scheduling algorithms for `nami`.
//!
//! Phase 0 stub. The `BestWindowScheduler` (cleanest contiguous window
//! before the deadline) lands in a subsequent session.

#![deny(missing_docs)]
#![deny(rust_2018_idioms)]

mod error;

pub use error::{Error, Result};
