//! IP-based region detection for `nami`.
//!
//! Phase 0 stub. The geolocation client and IP-to-BA mapping land in a
//! subsequent session.

#![deny(missing_docs)]
#![deny(rust_2018_idioms)]

mod error;

pub use error::{Error, Result};
